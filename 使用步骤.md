使用 MkDocs 创建笔记并将其推送到 GitHub 是一个非常常见的工作流程，尤其适合于创建文档网站。以下是详细的步骤指导：

### 1. 安装 MkDocs

首先确保你已经安装了 MkDocs。如果还没有，请在命令行中运行以下命令来安装：

bash深色版本

```
pip install mkdocs
```

### 2. 创建 MkDocs 项目

使用 `mkdocs new` 命令来创建一个新的 MkDocs 项目：

bash深色版本

```
mkdocs new my-notes
cd my-notes
```

这会创建一个名为 `my-notes` 的文件夹，里面包含了两个文件：`docs/index.md` 和 `mkdocs.yml`。

### 3. 编写内容

在 `docs/` 文件夹中添加或编辑 Markdown 文件（`.md`），以编写你的笔记内容。

### 4. 预览你的网站

要在本地预览你的 MkDocs 网站，可以使用以下命令启动开发服务器：

bash深色版本

```
mkdocs serve
```

默认情况下，你的网站将会在 `http://127.0.0.1:8000/` 上提供服务。你可以通过浏览器访问该地址查看你的笔记。

### 5. 构建静态站点

当你完成笔记后，可以使用 `mkdocs build` 命令构建你的网站。这会在项目的根目录下生成一个 `site/` 文件夹，包含所有需要上传到服务器上的静态文件。

bash深色版本

```
mkdocs build
```

### 6. 初始化 Git 仓库

进入你的 MkDocs 项目目录，并初始化为 Git 仓库：

bash深色版本

```
git init
```

然后，添加所有文件到 Git 仓库，并进行首次提交：

bash深色版本

```
git add .
git commit -m "Initial commit"
```

使用 SSH URL 进行 Git 操作

确保你的本地 Git 仓库关联的是 GitHub 仓库的 SSH URL 而不是 HTTPS URL。可以通过以下命令检查和设置：

bash深色版本

```
git remote -v
```

如果显示的是 HTTPS URL，你可以将其更改为 SSH URL：

bash深色版本

```
git remote set-url origin git@github.com:username/repository.git
```

请将 `username` 和 `repository` 替换为你的 GitHub 用户名和仓库名称。



### 7. 创建 GitHub 仓库并将代码推送到 GitHub

前往 GitHub 创建一个新的仓库。之后，将本地仓库与 GitHub 远程仓库关联，并推送代码：

bash深色版本

```
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main
git push -u origin main
```

请记得替换上述命令中的 `https://github.com/你的用户名/你的仓库名.git` 为你实际的 GitHub 仓库 URL。

### 8. 使用 MkDocs 将网站部署到 GitHub Pages

为了将 MkDocs 生成的静态网站直接部署到 GitHub Pages，你可以使用 `mkdocs gh-deploy` 命令。这需要你在系统上安装 `git` 并配置好 SSH keys。

首先，确保你的 `mkdocs.yml` 文件中包含如下配置，以指定部署分支：

yaml深色版本

```
site_name: 我的笔记
repo_url: https://github.com/你的用户名/你的仓库名
use_directory_urls: false
gh_deploy:
    cname: 你的自定义域名 # 如果有自定义域名的话
```

然后，运行以下命令来部署：

bash深色版本

```
mkdocs gh-deploy --clean
```

这个命令会自动构建你的网站并将其推送到 GitHub 仓库的 `gh-pages` 分支，GitHub Pages 会从这个分支来展示你的网站。